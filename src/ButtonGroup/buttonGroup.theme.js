export function buttonGroup({
  animation,
  border,
  colors,
  depth,
  font,
  spacing
}) {
  return {
    display: 'inline-flex',
    verticalAlign: 'middle',
    position: 'relative',
    stretch: 'flex'
  }
}
